import React, { useContext } from "react";
import { ScrollContext } from "./ScrollObserver";

export default function ParallaxProblem() {
  const {scrollY} = useContext(ScrollContext)
  

  
  return <h1>hello</h1>;
}
